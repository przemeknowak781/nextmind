{
  "name": "next-mind-academy",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Next Mind Academy — szkolenia AI z dofinansowaniem dla pracowników biurowych",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node scripts/check-placeholders.mjs && node scripts/fetch-bur.mjs && astro check && astro build && node scripts/rewrite-base.mjs",
    "build:nocheck": "node scripts/fetch-bur.mjs && astro build && node scripts/rewrite-base.mjs",
    "build:domain": "SITE_BASE= node scripts/check-placeholders.mjs && SITE_BASE= node scripts/fetch-bur.mjs && SITE_BASE= astro check && SITE_BASE= astro build --outDir ./dist-domain && SITE_BASE= DIST_DIR=./dist-domain node scripts/rewrite-base.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "check:content": "node scripts/check-placeholders.mjs",
    "bur:sync": "node scripts/fetch-bur.mjs",
    "bur:probe": "node scripts/fetch-bur.mjs --probe"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/mdx": "^4.0.5",
    "@astrojs/sitemap": "^3.2.1",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.1.0",
    "tailwindcss": "^4.0.0",
    "typescript": "^5.7.2",
    "zod": "^3.24.1"
  }
}
